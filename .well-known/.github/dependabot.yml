version: 2
updates:
  # Keep GitHub Actions (used by the workflows) patched against known issues.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "security"
